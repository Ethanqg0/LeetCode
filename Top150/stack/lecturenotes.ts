/*
    Recursion vs Tail Recursion
    Recursion is notorious for calling stack overflow because of the large amount of stored execution calls on the stack
    Tail Recursion avoids this by reusing the stack frame of the current function call for the next function call
*/

So 